<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissão de Internet -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskNotesApp"
        tools:targetApi="30">
        <activity
            android:name=".activities.UsuariosActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.tasks.UpdateTaskActivity"
            android:exported="false" />
        <activity
            android:name=".activities.tasks.CreateTaskActivity"
            android:exported="false" />
        <activity
            android:name=".activities.notes.UpdateNoteActivity"
            android:exported="false" />
        <activity
            android:name=".activities.notes.CreateNoteActivity"
            android:exported="false" />
        <activity
            android:name=".activities.notes.NoteActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskNotesApp" />
        <activity
            android:name=".activities.tasks.TaskActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MenuActivity"
            android:exported="true"></activity>
    </application>

</manifest>